[tox]
envlist = py27, py34

[testenv]

deps=-rrequirements.dev.txt
setenv=PYTHONPATH=lib/
commands=
    flake8
    py.test

[flake8]
exclude = ./doc/*,.tox/*,.git/*

[pytest]
addopts = --doctest-modules --ignore=setup.py --cov confspec
